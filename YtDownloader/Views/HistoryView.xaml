<UserControl x:Class="YtDownloader.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:YtDownloader.Converters"
             xmlns:models="clr-namespace:YtDownloader.Models">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolVis"/>
        <conv:NullToVisibilityConverter x:Key="NullVis"/>
        <conv:NullToVisibilityConverter x:Key="EmptyListVis"
                                        NullValue="Visible"
                                        NonNullValue="Collapsed"/>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       Text="Historial de descargas"
                       Foreground="{StaticResource TextPrimary}"
                       FontSize="{StaticResource FontMD}"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Style="{StaticResource GhostButton}"
                        Content="⬇  Exportar CSV"
                        Command="{Binding ExportToCsvCommand}"
                        FontSize="{StaticResource FontSM}"
                        Padding="12,6"
                        Margin="0,0,8,0"/>
                <Button Style="{StaticResource GhostButton}"
                        Content="🗑  Borrar todo"
                        Command="{Binding ClearHistoryCommand}"
                        FontSize="{StaticResource FontSM}"
                        Padding="12,6"/>
            </StackPanel>
        </Grid>

        <!-- Search bar -->
        <TextBox Grid.Row="1"
                 Style="{StaticResource ModernTextBox}"
                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,16"
                 Height="40"
                 Tag="Buscar por título o plataforma…"/>

        <!-- Empty state -->
        <Border Grid.Row="2"
                Background="{StaticResource BgSurface}"
                CornerRadius="10"
                Visibility="{Binding Entries.Count, Converter={StaticResource EmptyListVis}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="🕘" FontSize="40"
                           HorizontalAlignment="Center"
                           Foreground="{StaticResource TextMuted}"/>
                <TextBlock Text="Sin historial todavía"
                           Foreground="{StaticResource TextMuted}"
                           FontSize="{StaticResource FontBase}"
                           Margin="0,10,0,4"/>
                <TextBlock Text="Las descargas completadas aparecerán aquí"
                           Foreground="{StaticResource TextMuted}"
                           FontSize="{StaticResource FontSM}"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- History list -->
        <ScrollViewer Grid.Row="2"
                      Style="{StaticResource MinimalScrollViewer}"
                      Visibility="{Binding Entries.Count, Converter={StaticResource NullVis}}">
            <ItemsControl ItemsSource="{Binding Entries}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:HistoryEntry}">
                        <Border Background="{StaticResource BgCard}"
                                CornerRadius="8"
                                Padding="14,12"
                                Margin="0,0,0,8">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{StaticResource BgHover}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Thumbnail -->
                                <Border Grid.Column="0"
                                        Width="80" Height="48"
                                        CornerRadius="6"
                                        ClipToBounds="True"
                                        Margin="0,0,14,0">
                                    <Image Source="{Binding ThumbnailUrl}"
                                           Stretch="UniformToFill"/>
                                </Border>

                                <!-- Metadata -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Title}"
                                               Foreground="{StaticResource TextPrimary}"
                                               FontSize="{StaticResource FontBase}"
                                               FontWeight="Medium"
                                               TextTrimming="CharacterEllipsis"
                                               ToolTip="{Binding Title}"
                                               Margin="0,0,0,4"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <Border Background="{StaticResource BgHover}"
                                                CornerRadius="4" Padding="6,2"
                                                Margin="0,0,10,0">
                                            <TextBlock Text="{Binding Platform}"
                                                       Foreground="{StaticResource Accent}"
                                                       FontSize="{StaticResource FontXS}"
                                                       FontWeight="SemiBold"/>
                                        </Border>
                                        <TextBlock Foreground="{StaticResource TextMuted}"
                                                   FontSize="{StaticResource FontXS}"
                                                   Margin="0,0,16,0">
                                            <Run Text="⏱ "/>
                                            <Run Text="{Binding FormattedDuration, Mode=OneWay}"/>
                                        </TextBlock>
                                        <TextBlock Foreground="{StaticResource TextMuted}"
                                                   FontSize="{StaticResource FontXS}">
                                            <Run Text="💾 "/>
                                            <Run Text="{Binding FileSizeLabel, Mode=OneWay}"/>
                                        </TextBlock>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding FormatLabel}"
                                                   Foreground="{StaticResource TextSecondary}"
                                                   FontSize="{StaticResource FontXS}"
                                                   Margin="0,0,8,0"/>
                                        <TextBlock Text="•"
                                                   Foreground="{StaticResource TextMuted}"
                                                   FontSize="{StaticResource FontXS}"
                                                   Margin="0,0,8,0"/>
                                        <TextBlock Foreground="{StaticResource TextMuted}"
                                                   FontSize="{StaticResource FontXS}"
                                                   Text="{Binding DownloadedAt, StringFormat='dd/MM/yyyy HH:mm'}"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Actions -->
                                <StackPanel Grid.Column="2" Orientation="Horizontal"
                                            VerticalAlignment="Center">
                                    <Button Style="{StaticResource IconButton}"
                                            Command="{Binding DataContext.OpenFileCommand,
                                                RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="Abrir archivo"
                                            Margin="0,0,4,0">
                                        <TextBlock Text="▶" FontSize="11"
                                                   Foreground="{StaticResource TextSecondary}"/>
                                    </Button>
                                    <Button Style="{StaticResource IconButton}"
                                            Command="{Binding DataContext.OpenFolderCommand,
                                                RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="Abrir carpeta"
                                            Margin="0,0,4,0">
                                        <TextBlock Text="📁" FontSize="12"/>
                                    </Button>
                                    <Button Style="{StaticResource IconButton}"
                                            Command="{Binding DataContext.CopyUrlCommand,
                                                RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="Copiar URL"
                                            Margin="0,0,4,0">
                                        <TextBlock Text="🔗" FontSize="12"/>
                                    </Button>
                                    <Button Style="{StaticResource IconButton}"
                                            Command="{Binding DataContext.DeleteEntryCommand,
                                                RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="Eliminar del historial">
                                        <TextBlock Text="🗑" FontSize="12"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
